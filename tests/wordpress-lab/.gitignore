# Downloaded WordPress core + SQLite driver + PHP-WASM runtime are large and
# reproducible via `node setup.mjs`. Never commit them.
/wp/
/wp-sqlite/
/node_modules/
*.sqlite
*.sqlite-journal

# Rendered UI previews (regenerated, not source).
.preview/
